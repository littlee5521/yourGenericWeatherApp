class submitLocation {
     submitBtn:Element = document.querySelector('.searchbar__button')
     searchBar:Element = document.querySelector('.searchbar__input')

     setSubmitEvent() {
        this.submitBtn.addEventListener('click', () =>{
            
        })
     }
}